import React from 'react';

const Dashboard = () => {
  return (
    <div>
      <h2 className="text-2xl font-semibold mb-4">Dashboard</h2>
      <p>Welcome to the admin panel. Use the navigation bar to manage champions, teams, or matches.</p>
    </div>
  );
};

export default Dashboard;